/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.ctfassets.net',
      'storage.example.com',
      'files.tigris.dev'
    ],
  },
}

module.exports = nextConfig
